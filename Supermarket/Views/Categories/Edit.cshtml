@model Category; //Here we define type of data (@model dataType) like we are saying here that the type of the class which will eventually combine with HTML template here is of the type Category. @Model below is instance of Category and not the type anymore.


@section title{

	@if (Model != null)
	{
		<h3>Category : @Model.Name</h3>
	}

}

@if (Model != null)
{
	<partial name="_Category" model="@Model" />
}